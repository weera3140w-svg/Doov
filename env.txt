# เปลี่ยนชื่อไฟล์นี้เป็น .env ก่อนใช้งาน
# Rename this file to .env before running

SESSION_SECRET=doov_super_secret_key_change_this_in_production
PORT=3000
NODE_ENV=development
MAX_VIDEO_SIZE_MB=500
MAX_AVATAR_SIZE_MB=10
